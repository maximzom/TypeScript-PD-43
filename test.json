{
  "name": "PR2",
  "version": "1.0.0",
  "description": "Extended TypeScript demo with fetch, modals, scroll listeners and animations",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "start": "live-server --port=5500 ."
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "typescript": "^5.0.0"
  }
}
