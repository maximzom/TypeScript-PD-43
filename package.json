{
  "name": "usermanager-pro",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "build-all": "tsc src/app.ts --outDir dist && tsc src/users.ts --outDir dist && tsc src/posts.ts --outDir dist && tsc src/gallery.ts --outDir dist",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}